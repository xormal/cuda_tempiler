ПРОГОН САМОПРОВЕРКИ СТЕКА, 2026-08-01

================================================================================================
САМОПРОВЕРКИ, КОТОРЫМ НЕ НУЖНА КАРТА
Не покрыто здесь: ncu --selftest (карта+sudo), phases time (сборка+замер),
time на железе (нужна ПУСТАЯ карта), ccab bwd (~11 мин nvcc).
================================================================================================

>>> lint: ЗАКОН 90/90 + якорь по 4 отгруженным ядрам
    /opt/conda/miniconda3/bin/python3 ./tools/smem_lint.py --selftest
      | ############################################################################################################
      | # ИТОГ САМОПРОВЕРКИ: закон ОК, якорь ОК
      | ############################################################################################################

>>> residency: 5 якорей, включая 97.7 % попаданий в L2
    /opt/conda/miniconda3/bin/python3 ./tools/residency.py --selftest
      |     ОЦЕНКА по подгонке smem = 16 КБ + BQ*(2d+32). Ставьте --ctas-per-sm / --smem / --regs, если
      |     знаете точно. Вердикты якорей от этого не зависят (проверено перебором 1..4 CTA/SM).
      | 11. РАСЩЕПЛЕНИЕ ПО КЛЮЧАМ (split-K, num_splits>1) меняет сетку и состав волны -- не учтено.

>>> time: 24 проверки гейтов и оценивателя (карта не трогается)
    /opt/conda/miniconda3/bin/python3 ./tools/timeit.py --selftest
      |   ? частоты карты 9 НЕ ФИКСИРОВАЛИСЬ (lock_mhz не задан): дрейф ловится только измерением частоты по раундам, до замера ничего не гарантировано
      |   ВНИМАНИЕ: замечаний нет, но список выше НЕ ПУСТ -- это НЕ 'чисто'.
      | ИТОГ САМОПРОВЕРКИ: 24/24 пройдено

>>> phases: якорь долей фаз byte-forward 35.1/19.7/15.3/5.7/2.0 %
    /opt/conda/miniconda3/bin/python3 ./tools/phaseprof.py replay --times ./data/anchor_fwd_ws_phases.json
      |   * режим replay НИЧЕГО не собирает и не проверяет: он разбирает ЧУЖИЕ числа. Проверки фальсификаторов (сбитый хвост, подмена расписания) НЕ выполняли
      |   Пустой список подозрений при НЕПУСТОМ списке выше НЕ означает 'чисто':
      |   неполный инструмент даёт не меньше данных, а ДРУГОЙ ОТВЕТ с той же уверенностью.

>>> ccab: якорь Q(W) = 168/128/80/64
    /opt/conda/miniconda3/bin/python3 ./tools/cc_ab.py qtable
      |    W=48  Q=40    порог MaxLive (Q-7) = 33    временной порог (Q-2) = 38
      |    W=64  Q=32    порог MaxLive (Q-7) = 25    временной порог (Q-2) = 30
      | ЯКОРЬ 1: СОШЁЛСЯ 4/4

>>> ncu: отбор годного ncu (заглушки и 2025.x отвергаются)
    /opt/conda/miniconda3/bin/python3 ./tools/ncu.py --which
      | отвергнут /opt/conda/miniconda3/envs/qwen/bin/ncu                                -            launcher-ЗАГЛУШКА: 'Nsight Compute is not installed' (да
      | отвергнут /opt/conda/miniconda3/envs/cuda128/bin/ncu                             2025.1.1     слой 2: версия 2025.1.1 >= 2025.1 -- Volta снята с подде
      | ВЫБРАН: /opt/conda/miniconda3/pkgs/nsight-compute-2024.1.1.4-0/nsight-compute/2024.1.1/ncu

================================================================================================
  lint         ПРОШЛА                     ЗАКОН 90/90 + якорь по 4 отгруженным ядрам
  residency    ПРОШЛА                     5 якорей, включая 97.7 % попаданий в L2
  time         ПРОШЛА                     24 проверки гейтов и оценивателя (карта не трогается)
  phases       ПРОШЛА                     якорь долей фаз byte-forward 35.1/19.7/15.3/5.7/2.0 %
  ccab         ПРОШЛА                     якорь Q(W) = 168/128/80/64
  ncu          ПРОШЛА                     отбор годного ncu (заглушки и 2025.x отвергаются)
================================================================================================
ИТОГ: 6 из 6 прошли
НАПОМИНАНИЕ: пройденная самопроверка означает, что воспроизводится ЯКОРЬ, а не что
инструмент прав на вашем ядре. Раздел 'НЕ РАЗОБРАНО' каждого отчёта -- обязателен.

########## doctor ##########
================================================================================================
СОСТОЯНИЕ СРЕДЫ (ни один такт GPU не тратится, память карт не занимается)
================================================================================================

--- ИНТЕРПРЕТАТОРЫ ------------------------------------------------------------
  /opt/conda/miniconda3/bin/python3                    3.12.10
  /opt/conda/miniconda3/envs/vllm/bin/python           3.11.11

--- ИНСТРУМЕНТ -> ИНТЕРПРЕТАТОР (боевой режим) --------------------------------
  phases     phaseprof.py     есть       /opt/conda/miniconda3/bin/python3  [3.12.10]
  ncu        ncu.py           есть       /opt/conda/miniconda3/bin/python3  [3.12.10]
  lint       smem_lint.py     есть       /opt/conda/miniconda3/bin/python3  [3.12.10]
  residency  residency.py     есть       /opt/conda/miniconda3/bin/python3  [3.12.10 (требуется >= 3.12: PEP 701, перенос строки внутри f-строки)]
  ccab       cc_ab.py         есть       /opt/conda/miniconda3/bin/python3  [3.12.10]
  time       timeit.py        есть       /opt/conda/miniconda3/envs/vllm/bin/python  [3.11.11 + torch (боевой режим импортирует torch)]

--- ЧУЖИЕ ФАЙЛЫ, ОТ КОТОРЫХ ЗАВИСЯТ ЯКОРЯ -------------------------------------
  есть   тело якоря ncu (--selftest)
         ./tools/bankaudit.py
  есть   якорь phases replay
         ./data/anchor_fwd_ws_phases.json
  есть   якорь lint: 90 точек стенда
         ./data/mio_wavefronts.txt
  есть   репозиторий с отгруженными ядрами (lint, ccab, ncu-тела)
         ../VLLM_fa2/solutions/fa2_sm70_cutlass_grade
  есть   источник якорных долей фаз
         ../VLLM_fa2/solutions/fa2_sm70_cutlass_grade/docs/VOLTA_SM70.md
  есть   CUDA_HOME для nvcc/ptxas/cuobjdump
         /opt/conda/miniconda3/envs/cuda128
  есть   ЕДИНСТВЕННЫЙ годный ncu (2024.1.1)
         /opt/conda/miniconda3/pkgs/nsight-compute-2024.1.1.4-0/nsight-compute/2024.1.1/ncu

--- ИНСТРУМЕНТЫ СБОРКИ --------------------------------------------------------
  g++          /usr/bin/g++
  gcc          /usr/bin/gcc
  nvidia-smi   /usr/bin/nvidia-smi
  nvcc         /opt/conda/miniconda3/envs/cuda128/bin/nvcc
  ptxas        /opt/conda/miniconda3/envs/cuda128/bin/ptxas
  cuobjdump    /opt/conda/miniconda3/envs/cuda128/bin/cuobjdump

--- ЛОКАЛЬ (ncu CSV ломается о ru_RU: NBSP как разделитель тысяч) --------------
  LANG         en_US.UTF-8
  LC_ALL       ru_RU.UTF-8
  LC_NUMERIC   ka_GE.UTF-8
  CLI выставляет LC_ALL=C LANG=C всем дочерним процессам.

--- КАРТЫ (только опрос nvidia-smi) -------------------------------------------
  0, 51.01 W, 1530 MHz, 371 MiB
  1, 24.36 W, 135 MHz, 0 MiB
  2, 60.36 W, 1290 MHz, 29237 MiB
  3, 61.35 W, 1290 MHz, 29237 MiB
  чужих compute-процессов на машине: 3
  КАРТЫ 2 и 3 -- БОЕВОЙ СЕРВЕР. Не запускать, не убивать, память не занимать.
  НИКОГДА `pkill -f` по шаблону -- только по PID с карт 0-1.

--- СЕКРЕТЫ (значения НЕ печатаются) ------------------------------------------
  TEMPO_SUDO_PASS    не задан
  FA2_SUDO_PASS      не задан
  SUDO_PW            не задан

================================================================================================
НЕ УСТАНОВЛЕНО -- читать обязательно
================================================================================================
  * doctor проверяет НАЛИЧИЕ, а не ГОДНОСТЬ: что ncu 2024.1.1 существует -- проверено, что он
    сегодня профилирует эту карту -- НЕТ (это `ncu --which --deep`, ему нужны sudo и карта).
  * порог покоя карты (70 Вт) НЕ ЗАМЕРЕН на этих картах -- `time --calibrate-idle` на пустой.
  * что чужой процесс короче 100 мс сейчас НЕ идёт -- не проверено ничем: опрос дискретный.
  * версия torch/CUDA у интерпретатора vllm не сверялась с версией, которой собраны расширения.
  * doctor НЕ проверяет, что якоря воспроизводятся -- это `tempo_cli.py selftest`.
