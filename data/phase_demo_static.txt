==============================================================================
СТАТИЧЕСКИЙ РАЗБОР SASS ПО ФАЗАМ  (карта НЕ нужна)
==============================================================================
  Привязка через -lineinfo: у каждой команды SASS есть (файл, строка) исходника.
  Это счёт КОМАНД, а не времени: витки циклов не взвешены, задержки не учтены.

  БАЗА: 1888 команд SASS, регистров 63, разлив 0 Б

  фаза                 команд     доля
  ----------------------------------------
  gemm1                   520    27.5%
  softmax                  80     4.2%
  gemm2                   512    27.1%
  feed                     20     1.1%
  rendez                    4     0.2%
  epilogue                 68     3.6%
  ----------------------------------------
  ИТОГО названо          1204    63.8%
  НЕ ОТНЕСЕНО             684    36.2%   <-- команды, не попавшие ни в одну фазу
  БЕЗ ПРИВЯЗКИ              0     0.0%   <-- у команды нет строки (пролог/эпилог компилятора)

==============================================================================
ПРОВЕРКА ФАЛЬСИФИКАТОРОВ: не выбило ли снятие фазы X команды фазы Y
==============================================================================
  Строка = что снято. Столбец = сколько команд у фазы ОСТАЛОСЬ.
  Диагональ обязана упасть в ~0. ЛЮБОЙ заметный провал ВНЕ диагонали значит, что
  компилятор выбросил чужую работу -> доля снятой фазы ЗАВЫШЕНА, замер недействителен.

  снято \ фаза      gemm1  softmax    gemm2     feed   rendez epilogue    всего     рег
  -------------------------------------------------------------------------------------
  (база)              520       80      512       20        4       68     1888      63
  gemm1                 0       77      523       14        4       66     1080      55
  softmax             520        0      512       20        4       66     1800      56
  gemm2               520       80        0       14        4       68     1104      63
  feed                520       80      512        0        4       68     1880      63
  rendez              520       80      512       20        0       68     1880      63
  epilogue            520       80      512       20        4        0     1784      63

  ЦЕНА ПЛОМБЫ: 1856 -> 1888 команд (+1.72 %). Пломба стоит по одной команде PRMT на слово и присутствует В ОБОИХ вариантах, поэтому из отношения времён она в первом порядке сокращается.

==============================================================================
НЕ РАЗОБРАНО  --  читать ОБЯЗАТЕЛЬНО
==============================================================================
  * фаза rendez БЕЗ ПЛОМБЫ и без подстановки: при снятии компилятор вправе выбросить зависимый код ДРУГИХ фаз, и доля rendez будет ЗАВЫШЕНА
  * СБИТЫЙ ХВОСТ: снятие 'gemm1' убрало 30% команд фазы 'feed' (20 -> 14) -- доля 'gemm1' будет ЗАВЫШЕНА; поставьте пломбу FMHA_SEAL на выходы 'gemm1' и РАЗЛИЧНУЮ подстановку FMHA_PHASE_ELSE (одинаковые значения склеит CSE)
  * СБИТЫЙ ХВОСТ: снятие 'gemm2' убрало 30% команд фазы 'feed' (20 -> 14) -- доля 'gemm2' будет ЗАВЫШЕНА; поставьте пломбу FMHA_SEAL на выходы 'gemm2' и РАЗЛИЧНУЮ подстановку FMHA_PHASE_ELSE (одинаковые значения склеит CSE)
  * статический счёт НЕ взвешивает витки циклов: команда в теле главного цикла и команда в прологе весят здесь ОДИНАКОВО. Доли по времени берутся только из режима `time`.
  * команды, поднятые планировщиком через границу фазы, привязка относит к ИСХОДНОЙ строке, а стоят они в чужой фазе (замерено: LOP3 уезжает на ~16 позиций).

  Пустой список подозрений при НЕПУСТОМ списке выше НЕ означает 'чисто':
  неполный инструмент даёт не меньше данных, а ДРУГОЙ ОТВЕТ с той же уверенностью.
