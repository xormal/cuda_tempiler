### A: kESK=true (наш), без R7
объявленный бюджет: __launch_bounds__(256, 1) -> 255 рег/поток
======================================================================================================================
backward одной плиткой BI=128 BJ=64 MK=128  [без макросов]
======================================================================================================================
ядро                                      рег  стек   отчёт Б LDL/STL в цикле смем-ст смем-дин варпы вердикт
----------------------------------------------------------------------------------------------------------------------
attention_kernel_backward_batched_imp...  255   208   336/512 110/62  110/60        0    90624  8|8  РАЗЛИВ+СТЕНА-255
----------------------------------------------------------------------------------------------------------------------
колонки: рег = регистров/поток; стек = кадр стека, Б; отчёт = разлив ПО ОТЧЁТУ ptxas (store/load, Б);
         LDL/STL = ШТУК КОМАНД в теле (после пролога); в цикле = из них внутри обратных переходов;
         варпы = floor(65536/рег/32) | с учётом смем и потоков блока. 'отчёт 0/0' при ненулевых LDL/STL -- отчёт неполон.
     attention_kernel_backward_batched...: разлив есть -> REG(255) НЕ равно MaxLive, предсказание 'бюджет без разлива' НЕПРИМЕНИМО (нужна сборка с большим бюджетом).

НЕ РАЗОБРАНО (0):
  -- пусто. Функций в cubin: 1, команд SASS разобрано: 7064.
ПУСТОЙ СПИСОК ПРОБЛЕМ ПРИ НЕПУСТОМ СПИСКЕ НЕРАЗОБРАННОГО НЕ ОЗНАЧАЕТ 'ЧИСТО'.

### B: kESK=false (побайтово торчев вариант), без R7
объявленный бюджет: __launch_bounds__(256, 1) -> 255 рег/поток
======================================================================================================================
backward одной плиткой BI=128 BJ=64 MK=128  [без макросов]
======================================================================================================================
ядро                                      рег  стек   отчёт Б LDL/STL в цикле смем-ст смем-дин варпы вердикт
----------------------------------------------------------------------------------------------------------------------
attention_kernel_backward_batched_imp...  240     0       0/0   0/0     0/0         0    90624  8|8  ВЛЕЗАЕТ
----------------------------------------------------------------------------------------------------------------------
колонки: рег = регистров/поток; стек = кадр стека, Б; отчёт = разлив ПО ОТЧЁТУ ptxas (store/load, Б);
         LDL/STL = ШТУК КОМАНД в теле (после пролога); в цикле = из них внутри обратных переходов;
         варпы = floor(65536/рег/32) | с учётом смем и потоков блока. 'отчёт 0/0' при ненулевых LDL/STL -- отчёт неполон.
     attention_kernel_backward_batched...: MaxLive ~ 240 -> бюджет без разлива >= 247 рег; максимум варпов без разлива = 8 (Q(8)=255)

НЕ РАЗОБРАНО (0):
  -- пусто. Функций в cubin: 1, команд SASS разобрано: 6504.
ПУСТОЙ СПИСОК ПРОБЛЕМ ПРИ НЕПУСТОМ СПИСКЕ НЕРАЗОБРАННОГО НЕ ОЗНАЧАЕТ 'ЧИСТО'.
