[
 {
  "file": "fa2_src/fmha_kernel/kernel_backward.h",
  "md5": "ade82977bfd0426fc896fcb8bb577ce6",
  "edits": [
   {
    "anchor": "#include \"cutlass/layout/vector.h\"\n#include \"cutlass/numeric_conversion.h\"\n#include \"cutlass/numeric_types.h\"\n#include \"fused_qk_gradk.h\" // [CP1] hand-written interleaved QK||GradK mainloop (templated)\n#include \"cutlass/tensor_ref.h\"\n\n#include \"debug_utils.h\"\n#include \"gemm_kernel_utils.h\"\n",
    "replace": "#include \"cutlass/layout/vector.h\"\n#include \"cutlass/numeric_conversion.h\"\n#include \"cutlass/numeric_types.h\"\n#include \"fmha_phase_bwd.h\" // [PHASE] фазовая разметка (нулевая цена без -DFMHA_STRIP_MASK)\n#include \"fused_qk_gradk.h\" // [CP1] hand-written interleaved QK||GradK mainloop (templated)\n#include \"cutlass/tensor_ref.h\"\n#include \"debug_utils.h\"\n#include \"gemm_kernel_utils.h\"\n"
   },
   {
    "anchor": "    }\n\n    // Computes (dO*out).sum(-1) and writes it to `p.delta_ptr`\n    if (kKernelComputesDelta) {\n      constexpr int kOptimalElements =\n          128 / cutlass::sizeof_bits<scalar_t>::value;",
    "replace": "    }\n\n    // Computes (dO*out).sum(-1) and writes it to `p.delta_ptr`\n    // [PHASE] предпроход Delta: полный проход по dO и O ДО главного цикла. Пломба не нужна и не\n    // применима -- выход уходит в ГЛОБАЛЬНУЮ память, а её читатель (di) читает её же, поэтому\n    // выбросить фазу-потребителя компилятор не может; подстановка пустая намеренно.\n    FMHA_PHASE(delta, 10/*DELTA*/) {\n    if (kKernelComputesDelta) {\n      constexpr int kOptimalElements =\n          128 / cutlass::sizeof_bits<scalar_t>::value;"
   },
   {
    "anchor": "        }\n      }\n      __syncthreads();\n    }\n\n    OutputFragments output_frags;",
    "replace": "        }\n      }\n      __syncthreads();\n    }\n    }\n    FMHA_PHASE_ELSE(delta, 10/*DELTA*/) {\n      // намеренно пусто: Delta уходит в глобальную память, каскада вверх по течению нет\n    }\n\n    OutputFragments output_frags;"
   },
   {
    "anchor": "              warp_id, lane_id);\n        }\n      }\n      if (kOutputInRF) {\n        writeFragsToGmem<kKeysQueriesAlignedToBlockSize>(\n            shared_storage, output_frags, p, key_start, warp_id, lane_id);\n      } else if (getQueryStart(p, key_start) >= p.num_queries) {\n        zfillGradKV<kKeysQueriesAlignedToBlockSize>(\n            p, key_start, warp_id, lane_id);\n      }\n      __syncthreads();\n    }",
    "replace": "              warp_id, lane_id);\n        }\n      }\n      // [PHASE] выгрузка резидентных накопителей dK/dV наружу (путь d<=128). Тот же бит, что у\n      // круга через площадку при d>128: это ОДНА фаза \"материализация dK/dV\", просто две реализации.\n      FMHA_PHASE(kvout, 9/*KVOUT*/) {\n      if (kOutputInRF) {\n        writeFragsToGmem<kKeysQueriesAlignedToBlockSize>(\n            shared_storage, output_frags, p, key_start, warp_id, lane_id);\n      } else if (getQueryStart(p, key_start) >= p.num_queries) {\n        zfillGradKV<kKeysQueriesAlignedToBlockSize>(\n            p, key_start, warp_id, lane_id);\n      }\n      }\n      // ПОДСТАНОВКА: без выгрузки накопители dK/dV становятся мёртвыми, и компилятор унёс бы ОБА\n      // мейнлупа (GradV и GradK) -- снятой выгрузке приписались бы две чужие фазы.\n      FMHA_PHASE_ELSE(kvout, 9/*KVOUT*/) {\n        DSINK_ARR(p.grad_value_ptr, output_frags.gradV[0],\n                  int(MatmulGradV::Mma::FragmentC::kElements));\n        DSINK_ARR(p.grad_key_ptr, output_frags.gradK[0],\n                  int(MatmulGradK::Mma::FragmentC::kElements));\n      }\n      __syncthreads();\n    }"
   },
   {
    "anchor": "          (problem_size.k() + Mma::Shape::kK - 1) / Mma::Shape::kK;\n      __syncthreads();\n      // fused QK||GradK requires kMaxK == kBlockSizeI, i.e. exactly one column tile\n      mma(gemm_k_iterations, output_frags.gradK[0], iterator_B, output_frags.gradK[0]);\n      // [CP1 1c] trailing __syncthreads REMOVED so this GradK's drain overlaps the next tile's QK fill\n      // (anchored phase-1 merge). Safe: GradK's operands (tmp/tmpT + mm_gradK_deferred, all\n      // persistent) are disjoint from QK's (part1 mm_qk + part2 attn), and tmp/tmpT are not rewritten",
    "replace": "          (problem_size.k() + Mma::Shape::kK - 1) / Mma::Shape::kK;\n      __syncthreads();\n      // fused QK||GradK requires kMaxK == kBlockSizeI, i.e. exactly one column tile\n      FMHA_PHASE(gradk, 8/*GRADK*/) {\n      mma(gemm_k_iterations, output_frags.gradK[0], iterator_B, output_frags.gradK[0]);\n      }\n      DSEAL_ARR(output_frags.gradK[0], int(Mma::FragmentC::kElements));\n      // [CP1 1c] trailing __syncthreads REMOVED so this GradK's drain overlaps the next tile's QK fill\n      // (anchored phase-1 merge). Safe: GradK's operands (tmp/tmpT + mm_gradK_deferred, all\n      // persistent) are disjoint from QK's (part1 mm_qk + part2 attn), and tmp/tmpT are not rewritten"
   },
   {
    "anchor": "              p.query_ptr + gradk_query_start * p.q_strideM,\n              {num_queries_in_block, p.head_dim},\n              thread_id, no_offset);\n          fmha_fused::fused_qk_gradk(\n              mma, iterator_A, iterator_B, accum,\n              gk_mma, gk_iter_B, output_frags.gradK[0], gemm_k_iterations);\n        } else {\n          mma(gemm_k_iterations, accum, iterator_A, iterator_B, accum);\n        }\n      } else {\n        mma(gemm_k_iterations, accum, iterator_A, iterator_B, accum);\n      }\n      accum = cutlass::multiplies<typename Mma::FragmentC>()(scale, accum);\n\n      // Epilogue: add LSE + exp and store that to our shared memory buffer\n      // shmem <- (matmul_result -",
    "replace": "              p.query_ptr + gradk_query_start * p.q_strideM,\n              {num_queries_in_block, p.head_dim},\n              thread_id, no_offset);\n          // [PHASE] на СЛИТОМ пути QK и GradK делят ОДИН мейнлуп, поэтому размечены не здесь, а\n          // ВНУТРИ fused_qk_gradk.h (по каждой группе операций отдельно): снимать их блоком здесь\n          // значило бы менять форму самого QK (слитая против одиночной) -- это подмена расписания,\n          // а не снятие фазы.\n          fmha_fused::fused_qk_gradk(\n              mma, iterator_A, iterator_B, accum,\n              gk_mma, gk_iter_B, output_frags.gradK[0], gemm_k_iterations);\n          DSEAL_ARR(output_frags.gradK[0],\n                    int(MatmulGradK::Mma::FragmentC::kElements));\n        } else {\n          FMHA_PHASE(qk, 0/*QK*/) {\n            mma(gemm_k_iterations, accum, iterator_A, iterator_B, accum);\n          }\n        }\n      } else {\n        FMHA_PHASE(qk, 0/*QK*/) {\n          mma(gemm_k_iterations, accum, iterator_A, iterator_B, accum);\n        }\n      }\n      // ПЛОМБА выхода QK: без неё снятый QK оставляет S нулём, максимум сворачивается в константу,\n      // exp2 -- следом, и снятой фазе приписывается ВЕСЬ софтмакс с обоими зависимыми умножениями.\n      DSEAL_ARR(accum, int(Mma::FragmentC::kElements));\n      FMHA_PHASE(softmax, 1/*SOFTMAX*/) {\n      accum = cutlass::multiplies<typename Mma::FragmentC>()(scale, accum);\n      }\n\n      // Epilogue: add LSE + exp and store that to our shared memory buffer\n      // shmem <- (matmul_result -"
   },
   {
    "anchor": "      }\n\n      // Apply mask\n      if (p.custom_mask_type == CausalFromTopLeft ||\n          p.custom_mask_type == CausalFromBottomRight) {\n        auto lane_offset = MatmulQK::AccumLambdaIterator::get_lane_offset(",
    "replace": "      }\n\n      // Apply mask\n      FMHA_PHASE(softmax, 1/*SOFTMAX*/) {\n      if (p.custom_mask_type == CausalFromTopLeft ||\n          p.custom_mask_type == CausalFromBottomRight) {\n        auto lane_offset = MatmulQK::AccumLambdaIterator::get_lane_offset("
   },
   {
    "anchor": "            },\n            [&](int accum_m) {});\n      }\n\n      __syncthreads();\n      if (kPrologueGV) {\n        prologueGradV(0);\n      }\n      if (kPrologueDOV) {\n        prologueDOV();\n      }\n\n      MatmulQK::B2bGemm::accumApplyLSEToSmem(\n          shared_storage.attn_shared_storage(),\n          accum,",
    "replace": "            },\n            [&](int accum_m) {});\n      }\n      }\n\n      // РАНДЕВУ ОСТАВЛЕНО СНАРУЖИ ФАЗЫ: снятая фаза не должна уносить защёлку, иначе мерится\n      // \"фаза плюс защёлка\", а на 8 варпах это разные величины.\n      __syncthreads();\n      if (kPrologueGV) {\n        prologueGradV(0);\n      }\n      if (kPrologueDOV) {\n        prologueDOV();\n      }\n\n      FMHA_PHASE(softmax, 1/*SOFTMAX*/) {\n      MatmulQK::B2bGemm::accumApplyLSEToSmem(\n          shared_storage.attn_shared_storage(),\n          accum,"
   },
   {
    "anchor": "          warp_id,\n          lane_id,\n          output_tile_coords);\n#if 0\n      auto accum_ref_attnT = shared_storage.attn_shared_storage().accum_ref();\n      PRINT_TENSOR4x4_T0_L0(\"attn_T\", accum_ref_attnT);",
    "replace": "          warp_id,\n          lane_id,\n          output_tile_coords);\n      }\n      // ПОДСТАНОВКА: у снятого софтмакса пропадает ЕДИНСТВЕННЫЙ потребитель S, и компилятор унёс бы\n      // вместе с ним весь мейнлуп QK -- доля софтмакса тогда включила бы чужую фазу целиком.\n      FMHA_PHASE_ELSE(softmax, 1/*SOFTMAX*/) {\n        DSINK_ARR(p.grad_key_ptr, accum, int(Mma::FragmentC::kElements));\n      }\n#if 0\n      auto accum_ref_attnT = shared_storage.attn_shared_storage().accum_ref();\n      PRINT_TENSOR4x4_T0_L0(\"attn_T\", accum_ref_attnT);"
   },
   {
    "anchor": "      AccumTileGmem gmem_tile{\n          reinterpret_cast<fmha_accum_storage_t*>(p.workspace_gv) +\n          storage_id * AccumTileGmem::kElementsStored};\n      if (!kOutputInRF) {\n        if (isFirstQuery || !kNeedsAccumGradV) {\n          output_frags.gradV[fragV].clear();\n        } else {\n          gmem_tile.load(output_frags.gradV[fragV], thread_id);\n        }\n      }\n      mma.set_prologue_done(kPrologueGV);\n",
    "replace": "      AccumTileGmem gmem_tile{\n          reinterpret_cast<fmha_accum_storage_t*>(p.workspace_gv) +\n          storage_id * AccumTileGmem::kElementsStored};\n      // [PHASE] круг накопителя dV через площадку в глобальной памяти -- вход (при d>128 это\n      // ~90 % трафика ядра; при d<=128 накопитель резидентен и весь блок компилируется в ничто).\n      FMHA_PHASE(kvout, 9/*KVOUT*/) {\n      if (!kOutputInRF) {\n        if (isFirstQuery || !kNeedsAccumGradV) {\n          output_frags.gradV[fragV].clear();\n        } else {\n          gmem_tile.load(output_frags.gradV[fragV], thread_id);\n        }\n      }\n      }\n      FMHA_PHASE_ELSE(kvout, 9/*KVOUT*/) {\n        // без круга накопитель обязан быть определён -- иначе снятие KVOUT унесёт и GradV\n        output_frags.gradV[fragV].clear();\n      }\n      mma.set_prologue_done(kPrologueGV);\n"
   },
   {
    "anchor": "      // Compute threadblock-scoped matrix multiply-add\n      __syncthreads();\n\n      mma(gemm_k_iterations,\n          output_frags.gradV[fragV],\n          iterator_B,\n          output_frags.gradV[fragV]);\n      __syncthreads();\n      if (kPrologueGV && !kSingleIterationGradV &&\n          col + MatmulGradV::ThreadblockShape::kN < p.head_dim_value) {\n        prologueGradV(col + MatmulGradV::ThreadblockShape::kN);\n      }\n\n      if (!kOutputInRF) {\n        if (kNeedsAccumGradV && !isLastQuery) {\n          gmem_tile.store(output_frags.gradV[fragV], thread_id);",
    "replace": "      // Compute threadblock-scoped matrix multiply-add\n      __syncthreads();\n\n      FMHA_PHASE(gradv, 2/*GRADV*/) {\n      mma(gemm_k_iterations,\n          output_frags.gradV[fragV],\n          iterator_B,\n          output_frags.gradV[fragV]);\n      }\n      // ПЛОМБА выхода dV: без неё снятый GradV оставляет накопитель константой, и эпилог/круг\n      // накопителя (KVOUT) свернётся вместе с ним.\n      DSEAL_ARR(output_frags.gradV[fragV], int(Mma::FragmentC::kElements));\n      __syncthreads();\n      if (kPrologueGV && !kSingleIterationGradV &&\n          col + MatmulGradV::ThreadblockShape::kN < p.head_dim_value) {\n        prologueGradV(col + MatmulGradV::ThreadblockShape::kN);\n      }\n\n      // [PHASE] круг накопителя dV -- выход (запись в площадку либо эпилог в grad_value)\n      FMHA_PHASE(kvout, 9/*KVOUT*/) {\n      if (!kOutputInRF) {\n        if (kNeedsAccumGradV && !isLastQuery) {\n          gmem_tile.store(output_frags.gradV[fragV], thread_id);"
   },
   {
    "anchor": "              warp_id,\n              lane_id);\n        }\n      }\n    }\n    __syncthreads();",
    "replace": "              warp_id,\n              lane_id);\n        }\n      }\n      }\n      FMHA_PHASE_ELSE(kvout, 9/*KVOUT*/) {\n        // ПОДСТАНОВКА: у снятого круга пропадает единственный потребитель dV -> унесло бы GradV\n        DSINK_ARR(p.grad_value_ptr, output_frags.gradV[fragV],\n                  int(Mma::FragmentC::kElements));\n      }\n    }\n    __syncthreads();"
   },
   {
    "anchor": "          (p.head_dim_value + Mma::Shape::kK - 1) / Mma::Shape::kK;\n\n      // Compute threadblock-scoped matrix multiply-add\n      mma(gemm_k_iterations, accum, iterator_A, iterator_B, accum);\n      __syncthreads();\n      if (kPrologueGQ) {\n        prologueGradQ(0);",
    "replace": "          (p.head_dim_value + Mma::Shape::kK - 1) / Mma::Shape::kK;\n\n      // Compute threadblock-scoped matrix multiply-add\n      FMHA_PHASE(doivj, 3/*DOIVJ*/) {\n      mma(gemm_k_iterations, accum, iterator_A, iterator_B, accum);\n      }\n      // ПЛОМБА выхода dP: снятый DOIVJ оставляет dP нулём, и (0 - Di)*P свернулось бы в\n      // константу вместе со всей поэлементной фазой DS.\n      DSEAL_ARR(accum, int(Mma::FragmentC::kElements));\n      __syncthreads();\n      if (kPrologueGQ) {\n        prologueGradQ(0);"
   },
   {
    "anchor": "#endif\n        accum_t current_di;\n        // dSij = (dPij - Di) * Pij\n        LambdaIterator::iterateRows(\n            lane_offset,\n            [&](int accum_m) { current_di = shared_storage.di()[accum_m]; },",
    "replace": "#endif\n        accum_t current_di;\n        // dSij = (dPij - Di) * Pij\n        FMHA_PHASE(ds, 4/*DS*/) {\n        LambdaIterator::iterateRows(\n            lane_offset,\n            [&](int accum_m) { current_di = shared_storage.di()[accum_m]; },"
   },
   {
    "anchor": "            [&](int accum_m) {\n\n            });\n\n        // store bias gradient tile dBij to global memory,\n        // where dBij = dSij = Pij * (dPij - Di)",
    "replace": "            [&](int accum_m) {\n\n            });\n        }\n\n        // store bias gradient tile dBij to global memory,\n        // where dBij = dSij = Pij * (dPij - Di)"
   },
   {
    "anchor": "          epilogue(output_op, output_iter, accum, output_iter);\n        }\n\n        accum = accum * scale;\n\n#if 0\n        PRINT_B0_T0(\"(doivj - di) * attn * scale\");\n        print_warp_accum<LambdaIterator>(accum, lane_offset, 4, 4);\n#endif\n\n        __syncthreads();\n        if (!MatmulGradK::DefaultMmaFromSmem::kIsTransposedA) {\n          auto tmpT = shared_storage.tmpT_shared_storage().accum_ref();\n          // attn <- attn_T.T",
    "replace": "          epilogue(output_op, output_iter, accum, output_iter);\n        }\n\n        FMHA_PHASE(ds, 4/*DS*/) {\n        accum = accum * scale;\n        }\n        // ПЛОМБА выхода dS: снятое поэлементное умножение оставило бы dS равным dP, что само по\n        // себе безобидно, НО при снятой паре (DS+DOIVJ) даёт константу и уносит укладку.\n        DSEAL_ARR(accum, int(Mma::FragmentC::kElements));\n\n#if 0\n        PRINT_B0_T0(\"(doivj - di) * attn * scale\");\n        print_warp_accum<LambdaIterator>(accum, lane_offset, 4, 4);\n#endif\n\n        __syncthreads();\n        // [PHASE] МАТЕРИАЛИЗАЦИЯ dS/dS^T в разделяемую: транспонирующая запись по .at() -- главный\n        // подозреваемый по конфликтности банков (у бэкварда 34.6 % трафика разделяемой -- конфликт).\n        FMHA_PHASE(dsstore, 5/*DSSTORE*/) {\n        if (!MatmulGradK::DefaultMmaFromSmem::kIsTransposedA) {\n          auto tmpT = shared_storage.tmpT_shared_storage().accum_ref();\n          // attn <- attn_T.T"
   },
   {
    "anchor": "              [&](int accum_m) {});\n        }\n#endif\n      }\n\n#ifndef FMHA_BWD_R7\n      MatmulDOIVJ::B2bGemm::accumToSmem(\n          shared_storage.tmp_shared_storage(),",
    "replace": "              [&](int accum_m) {});\n        }\n#endif\n        }\n        // ПОДСТАНОВКА: у снятой укладки пропадает единственный потребитель dS, и компилятор унёс бы\n        // ВМЕСТЕ с ней и DS, и весь мейнлуп DOIVJ -- доля укладки включила бы две чужие фазы.\n        FMHA_PHASE_ELSE(dsstore, 5/*DSSTORE*/) {\n          DSINK_ARR(p.grad_key_ptr, accum, int(Mma::FragmentC::kElements));\n        }\n      }\n\n      FMHA_PHASE(dsstore, 5/*DSSTORE*/) {\n#ifndef FMHA_BWD_R7\n      MatmulDOIVJ::B2bGemm::accumToSmem(\n          shared_storage.tmp_shared_storage(),"
   },
   {
    "anchor": "            output_tile_coords);\n      }\n#endif\n      __syncthreads();\n    }\n    // Force `nvcc` to recompute values that depend on the variables just below",
    "replace": "            output_tile_coords);\n      }\n#endif\n      }\n      __syncthreads();\n    }\n    // Force `nvcc` to recompute values that depend on the variables just below"
   },
   {
    "anchor": "      }\n\n      AccumTileGmem gmem_tile{&p.workspace_gq[storage_id].buffer[0]};\n      if (!kNeedsAccumGradQ ||\n          (p.num_splits_key_device() == 1 && key_start == 0)) {\n        // if we know we are the first to access it, we know it's only zeros.\n        // Avoids a load from gmem (and gmem init as well)\n        accum.clear();\n      } else {\n        gmem_tile.load(accum, thread_id);\n      }\n\n      auto gemm_k_iterations =\n          (problem_size.k() + Mma::Shape::kK - 1) / Mma::Shape::kK;\n\n      // Compute threadblock-scoped matrix multiply-add\n      __syncthreads();\n      mma.set_prologue_done(kPrologueGQ);\n      mma(gemm_k_iterations, accum, iterator_B, accum);\n      __syncthreads();\n      bool isLastColumn = kSingleIterationGradQ ||\n          (col + MatmulGradQ::ThreadblockShape::kN >= p.head_dim);",
    "replace": "      }\n\n      AccumTileGmem gmem_tile{&p.workspace_gq[storage_id].buffer[0]};\n      // [PHASE] вход круга накопителя dQ (площадка под замком) -- часть свёртки dQ\n      FMHA_PHASE(dqfold, 7/*DQFOLD*/) {\n      if (!kNeedsAccumGradQ ||\n          (p.num_splits_key_device() == 1 && key_start == 0)) {\n        // if we know we are the first to access it, we know it's only zeros.\n        // Avoids a load from gmem (and gmem init as well)\n        accum.clear();\n      } else {\n        gmem_tile.load(accum, thread_id);\n      }\n      }\n      FMHA_PHASE_ELSE(dqfold, 7/*DQFOLD*/) {\n        accum.clear();   // накопитель обязан быть определён и без круга\n      }\n\n      auto gemm_k_iterations =\n          (problem_size.k() + Mma::Shape::kK - 1) / Mma::Shape::kK;\n\n      // Compute threadblock-scoped matrix multiply-add\n      __syncthreads();\n      mma.set_prologue_done(kPrologueGQ);\n      FMHA_PHASE(gradq, 6/*GRADQ*/) {\n      mma(gemm_k_iterations, accum, iterator_B, accum);\n      }\n      // ПЛОМБА выхода dQ (колоночный lock-RMW путь d>128)\n      DSEAL_ARR(accum, int(Mma::FragmentC::kElements));\n      __syncthreads();\n      bool isLastColumn = kSingleIterationGradQ ||\n          (col + MatmulGradQ::ThreadblockShape::kN >= p.head_dim);"
   },
   {
    "anchor": "            numAddsSoFar == getNumParallelBlocksForQuery(p, query_start));\n        assert(numAddsSoFar <= getNumParallelBlocksForQuery(p, query_start));\n      }\n      if (kNeedsAccumGradQ && !isLast) {\n        gmem_tile.store(accum, thread_id);\n        if (p.num_splits_key_device() > 1) {",
    "replace": "            numAddsSoFar == getNumParallelBlocksForQuery(p, query_start));\n        assert(numAddsSoFar <= getNumParallelBlocksForQuery(p, query_start));\n      }\n      // [PHASE] выход круга накопителя dQ: запись в площадку под замком либо эпилог в grad_query\n      FMHA_PHASE(dqfold, 7/*DQFOLD*/) {\n      if (kNeedsAccumGradQ && !isLast) {\n        gmem_tile.store(accum, thread_id);\n        if (p.num_splits_key_device() > 1) {"
   },
   {
    "anchor": "            warp_id,\n            lane_id);\n      }\n    }\n    }\n#ifdef FMHA_BWD_R7",
    "replace": "            warp_id,\n            lane_id);\n      }\n      }\n      // ПОДСТАНОВКА: у снятой свёртки пропадает единственный потребитель dQ -> унесло бы GradQ\n      FMHA_PHASE_ELSE(dqfold, 7/*DQFOLD*/) {\n        DSINK_ARR(p.grad_query_ptr, accum, int(Mma::FragmentC::kElements));\n      }\n    }\n    }\n#ifdef FMHA_BWD_R7"
   },
   {
    "anchor": "          {r7_ps.k(), r7_ps.n()},\n          thread_id,\n          cutlass::MatrixCoord{0, 0});\n      R7Mma::template prologue_whole<true, false>(  // load ONLY k^T (A); S^T (B) already filled on-chip in DOIVJ\n          shared_storage.r7_kT(), shared_storage.r7_sT(),\n          iter_kT, iter_sT, thread_id, num_keys_in_block);\n      __syncthreads();\n      R7Mma mma(\n          shared_storage.r7_kT(), shared_storage.r7_sT(),",
    "replace": "          {r7_ps.k(), r7_ps.n()},\n          thread_id,\n          cutlass::MatrixCoord{0, 0});\n      // [PHASE] подача k^T -- часть канала умножения dQ (не отдельная фаза: без неё у мейнлупа нет\n      // операнда, а рандеву ниже остаётся снаружи разметки).\n      FMHA_PHASE(gradq, 6/*GRADQ*/) {\n      R7Mma::template prologue_whole<true, false>(  // load ONLY k^T (A); S^T (B) already filled on-chip in DOIVJ\n          shared_storage.r7_kT(), shared_storage.r7_sT(),\n          iter_kT, iter_sT, thread_id, num_keys_in_block);\n      }\n      __syncthreads();\n      R7Mma mma(\n          shared_storage.r7_kT(), shared_storage.r7_sT(),"
   },
   {
    "anchor": "      accum.clear();\n      auto r7_k_iters =\n          (num_keys_in_block + R7Mma::Shape::kK - 1) / R7Mma::Shape::kK;\n      mma(r7_k_iters, accum, iter_kT, iter_sT, accum);\n      int r7_warp_mn = warp_id %\n          (R7Mma::Base::WarpCount::kM * R7Mma::Base::WarpCount::kN);\n      auto r7_coords = cutlass::MatrixCoord{\n          r7_warp_mn % R7Mma::Base::WarpCount::kM,\n          r7_warp_mn / R7Mma::Base::WarpCount::kM};\n      auto r7_lo = MatmulGradQ::R7_AccumLambdaIterator::get_lane_offset(\n          lane_id, warp_id, r7_coords);\n#if defined(FMHA_BWD_COALESCE)\n      // [COALESCE, measured] Transpose accum[d,query] through an fp32 smem slab in [query,d] layout so the\n      // fold's per-word global RED is d-CONTIGUOUS (4 transaction-granules/request vs the raw scattered 16 -> the",
    "replace": "      accum.clear();\n      auto r7_k_iters =\n          (num_keys_in_block + R7Mma::Shape::kK - 1) / R7Mma::Shape::kK;\n      FMHA_PHASE(gradq, 6/*GRADQ*/) {\n      mma(r7_k_iters, accum, iter_kT, iter_sT, accum);\n      }\n      // ПЛОМБА выхода dQ^T: иначе снятый R7-мейнлуп оставит накопитель нулём и свёртка (RED) уйдёт\n      // вместе с ним -- снятому GradQ припишется ВСЯ свёртка dQ, которая по ncu и есть #1 простой.\n      DSEAL_ARR(accum, int(R7Mma::FragmentC::kElements));\n      int r7_warp_mn = warp_id %\n          (R7Mma::Base::WarpCount::kM * R7Mma::Base::WarpCount::kN);\n      auto r7_coords = cutlass::MatrixCoord{\n          r7_warp_mn % R7Mma::Base::WarpCount::kM,\n          r7_warp_mn / R7Mma::Base::WarpCount::kM};\n      auto r7_lo = MatmulGradQ::R7_AccumLambdaIterator::get_lane_offset(\n          lane_id, warp_id, r7_coords);\n      // [PHASE] СВЁРТКА dQ наружу: пачка глобальных RED (fragment-order либо рассеянная). По ncu\n      // это #1 простой ядра (lg_throttle), и в разложении она обязана стоять отдельно от умножения.\n      FMHA_PHASE(dqfold, 7/*DQFOLD*/) {\n#if defined(FMHA_BWD_COALESCE)\n      // [COALESCE, measured] Transpose accum[d,query] through an fp32 smem slab in [query,d] layout so the\n      // fold's per-word global RED is d-CONTIGUOUS (4 transaction-granules/request vs the raw scattered 16 -> the"
   },
   {
    "anchor": "            }\n          },\n          [&](int accum_m) {});\n      }\n#ifdef FMHA_BWD_R7_TICKET\n      // [Q2 closing-ticket, measured] PER-ARRIVAL finalizer: threshold = #key-blocks <= this query-band",
    "replace": "            }\n          },\n          [&](int accum_m) {});\n      }\n      }\n      // ПОДСТАНОВКА: без свёртки у dQ^T нет ни одного потребителя, и компилятор унёс бы весь\n      // мейнлуп GradQ вместе с подачей k^T -- снятой свёртке приписалось бы чужое умножение.\n      FMHA_PHASE_ELSE(dqfold, 7/*DQFOLD*/) {\n        DSINK_ARR(p.grad_query_ptr, accum, int(R7Mma::FragmentC::kElements));\n      }\n#ifdef FMHA_BWD_R7_TICKET\n      // [Q2 closing-ticket, measured] PER-ARRIVAL finalizer: threshold = #key-blocks <= this query-band"
   },
   {
    "anchor": "      AccumTileGmem gmem_tile{\n          reinterpret_cast<fmha_accum_storage_t*>(p.workspace) +\n          storage_id * AccumTileGmem::kElementsStored};\n      if (!kOutputInRF) {\n        if (isFirstQuery || !kNeedsAccumGradK) {\n          output_frags.gradK[fragK].clear();\n        } else {\n          gmem_tile.load(output_frags.gradK[fragK], thread_id);\n        }\n      }\n      mma.set_prologue_done(kPrologueGK);\n",
    "replace": "      AccumTileGmem gmem_tile{\n          reinterpret_cast<fmha_accum_storage_t*>(p.workspace) +\n          storage_id * AccumTileGmem::kElementsStored};\n      // [PHASE] круг накопителя dK через площадку -- вход\n      FMHA_PHASE(kvout, 9/*KVOUT*/) {\n      if (!kOutputInRF) {\n        if (isFirstQuery || !kNeedsAccumGradK) {\n          output_frags.gradK[fragK].clear();\n        } else {\n          gmem_tile.load(output_frags.gradK[fragK], thread_id);\n        }\n      }\n      }\n      FMHA_PHASE_ELSE(kvout, 9/*KVOUT*/) {\n        output_frags.gradK[fragK].clear();\n      }\n      mma.set_prologue_done(kPrologueGK);\n"
   },
   {
    "anchor": "      // Compute threadblock-scoped matrix multiply-add\n      __syncthreads();\n\n      mma(gemm_k_iterations,\n          output_frags.gradK[fragK],\n          iterator_B,\n          output_frags.gradK[fragK]);\n      __syncthreads();\n      bool isLastColumn = kSingleIterationGradK ||\n          col + MatmulGradK::ThreadblockShape::kN >= p.head_dim;",
    "replace": "      // Compute threadblock-scoped matrix multiply-add\n      __syncthreads();\n\n      FMHA_PHASE(gradk, 8/*GRADK*/) {\n      mma(gemm_k_iterations,\n          output_frags.gradK[fragK],\n          iterator_B,\n          output_frags.gradK[fragK]);\n      }\n      DSEAL_ARR(output_frags.gradK[fragK], int(Mma::FragmentC::kElements));\n      __syncthreads();\n      bool isLastColumn = kSingleIterationGradK ||\n          col + MatmulGradK::ThreadblockShape::kN >= p.head_dim;"
   },
   {
    "anchor": "      }\n\n      // Output results\n      if (!kOutputInRF) {\n        if (kNeedsAccumGradK && !isLastQuery) {\n          gmem_tile.store(output_frags.gradK[fragK], thread_id);",
    "replace": "      }\n\n      // Output results\n      // [PHASE] круг накопителя dK -- выход\n      FMHA_PHASE(kvout, 9/*KVOUT*/) {\n      if (!kOutputInRF) {\n        if (kNeedsAccumGradK && !isLastQuery) {\n          gmem_tile.store(output_frags.gradK[fragK], thread_id);"
   },
   {
    "anchor": "              lane_id);\n          __syncthreads();\n        }\n      }\n    }\n    }  // [CP1 deferred-Kbar] close the in-tile-GradK guard (non-deferred configs)",
    "replace": "              lane_id);\n          __syncthreads();\n        }\n      }\n      }\n      FMHA_PHASE_ELSE(kvout, 9/*KVOUT*/) {\n        DSINK_ARR(p.grad_key_ptr, output_frags.gradK[fragK],\n                  int(Mma::FragmentC::kElements));\n      }\n    }\n    }  // [CP1 deferred-Kbar] close the in-tile-GradK guard (non-deferred configs)"
   }
  ]
 },
 {
  "file": "fa2_src/fmha_kernel/fused_qk_gradk.h",
  "md5": "d61b512db49b0e520f45899d120b415a",
  "edits": [
   {
    "anchor": "//   QK  : s      = K @ Q^T ; streams A(K),B(Q) gmem->smem, 2-stage double buffer.\n//   GradK: gradK += T^T @ Q; A(=T) FIXED smem tile (warp-iter reads it, no wrap), only B(Q) streamed.\n// Transforms are identity (NumericArrayConverter same-type) -> stored raw. GradK's A-scale is disabled.\n#pragma once\n#include \"cutlass/cutlass.h\"\n\nnamespace fmha_fused {\n",
    "replace": "//   QK  : s      = K @ Q^T ; streams A(K),B(Q) gmem->smem, 2-stage double buffer.\n//   GradK: gradK += T^T @ Q; A(=T) FIXED smem tile (warp-iter reads it, no wrap), only B(Q) streamed.\n// Transforms are identity (NumericArrayConverter same-type) -> stored raw. GradK's A-scale is disabled.\n//\n// [PHASE] Здесь и только здесь QK и GradK разделяются на ФАЗЫ: на слитом пути они занимают ОДИН канал\n// в одном цикле, и снять их по отдельности можно лишь по группам операций (подача, укладка, продвижение\n// ступени, выдача HMMA). Общая защёлка __syncthreads() оставлена СНАРУЖИ обеих разметок: она принадлежит\n// не фазе, а рандеву блока, и её снятие мерило бы не фазу, а фазу плюс защёлку.\n// При маске 0 каждый FMHA_PHASE -- это `if constexpr (true)`, то есть та же самая последовательность.\n#pragma once\n#include \"cutlass/cutlass.h\"\n#include \"fmha_phase_bwd.h\"\n\nnamespace fmha_fused {\n"
   },
   {
    "anchor": "  // ---------------- Prologue (both), single shared sync ----------------\n  QkTbA qk_tb_A;\n  QkTbB qk_tb_B;\n  qk_tb_A.clear();\n  qk_tb_B.clear();\n  qk_iter_A.load(qk_tb_A);\n  qk_iter_B.load(qk_tb_B);\n  ++qk_iter_A;\n  ++qk_iter_B;\n  qk.fused_smem_iter_A().store(qk_tb_A);\n  qk.fused_smem_iter_B().store(qk_tb_B);\n  ++qk.fused_smem_iter_A();\n  ++qk.fused_smem_iter_B();\n\n  GkTbB gk_tb_B;\n  gk_tb_B.clear();\n  gk_iter_B.set_residual_tile(gemm_k_iterations == 1);\n  gk_iter_B.load(gk_tb_B);\n  ++gk_iter_B;\n  gk.fused_smem_iter_B().store(gk_tb_B);\n  ++gk.fused_smem_iter_B();\n\n  __syncthreads();\n\n  // ---------------- Load warp fragment [0] for both ----------------\n  QkWarpA qk_A[2];\n  QkWarpB qk_B[2];\n  qk.fused_warp_iter_A().set_kgroup_index(0);\n  qk.fused_warp_iter_B().set_kgroup_index(0);\n  qk.fused_warp_iter_A().load(qk_A[0]);\n  qk.fused_warp_iter_B().load(qk_B[0]);\n  ++qk.fused_warp_iter_A();\n  ++qk.fused_warp_iter_B();\n\n  GkWarpA gk_A[2];\n  GkWarpB gk_B[2];\n  gk_A[0].clear();\n  gk_B[0].clear();\n  gk.fused_warp_iter_B().set_kgroup_index(0);\n  gk.fused_warp_iter_A().load(gk_A[0]);\n  gk.fused_warp_iter_B().load(gk_B[0]);\n  ++gk.fused_warp_iter_A();\n  ++gk.fused_warp_iter_B();\n\n  qk_iter_A.clear_mask(gemm_k_iterations <= 1);\n  qk_iter_B.clear_mask(gemm_k_iterations <= 1);\n  gk_iter_B.set_residual_tile(gemm_k_iterations == 2);\n  gk_iter_B.clear_mask(gemm_k_iterations <= 1);\n\n  // ---------------- Interleaved mainloop ----------------\n  CUTLASS_GEMM_LOOP",
    "replace": "  // ---------------- Prologue (both), single shared sync ----------------\n  QkTbA qk_tb_A;\n  QkTbB qk_tb_B;\n  FMHA_PHASE(qk, 0/*QK*/) {\n    qk_tb_A.clear();\n    qk_tb_B.clear();\n    qk_iter_A.load(qk_tb_A);\n    qk_iter_B.load(qk_tb_B);\n    ++qk_iter_A;\n    ++qk_iter_B;\n    qk.fused_smem_iter_A().store(qk_tb_A);\n    qk.fused_smem_iter_B().store(qk_tb_B);\n    ++qk.fused_smem_iter_A();\n    ++qk.fused_smem_iter_B();\n  }\n\n  GkTbB gk_tb_B;\n  FMHA_PHASE(gradk, 8/*GRADK*/) {\n    gk_tb_B.clear();\n    gk_iter_B.set_residual_tile(gemm_k_iterations == 1);\n    gk_iter_B.load(gk_tb_B);\n    ++gk_iter_B;\n    gk.fused_smem_iter_B().store(gk_tb_B);\n    ++gk.fused_smem_iter_B();\n  }\n\n  __syncthreads();\n\n  // ---------------- Load warp fragment [0] for both ----------------\n  QkWarpA qk_A[2];\n  QkWarpB qk_B[2];\n  FMHA_PHASE(qk, 0/*QK*/) {\n    qk.fused_warp_iter_A().set_kgroup_index(0);\n    qk.fused_warp_iter_B().set_kgroup_index(0);\n    qk.fused_warp_iter_A().load(qk_A[0]);\n    qk.fused_warp_iter_B().load(qk_B[0]);\n    ++qk.fused_warp_iter_A();\n    ++qk.fused_warp_iter_B();\n  }\n\n  GkWarpA gk_A[2];\n  GkWarpB gk_B[2];\n  FMHA_PHASE(gradk, 8/*GRADK*/) {\n    gk_A[0].clear();\n    gk_B[0].clear();\n    gk.fused_warp_iter_B().set_kgroup_index(0);\n    gk.fused_warp_iter_A().load(gk_A[0]);\n    gk.fused_warp_iter_B().load(gk_B[0]);\n    ++gk.fused_warp_iter_A();\n    ++gk.fused_warp_iter_B();\n  }\n\n  FMHA_PHASE(qk, 0/*QK*/) {\n    qk_iter_A.clear_mask(gemm_k_iterations <= 1);\n    qk_iter_B.clear_mask(gemm_k_iterations <= 1);\n  }\n  FMHA_PHASE(gradk, 8/*GRADK*/) {\n    gk_iter_B.set_residual_tile(gemm_k_iterations == 2);\n    gk_iter_B.clear_mask(gemm_k_iterations <= 1);\n  }\n\n  // ---------------- Interleaved mainloop ----------------\n  CUTLASS_GEMM_LOOP"
   },
   {
    "anchor": "      bool gk_hasNext = true;\n\n      if (warp_mma_k == kWarpGemmIterations - 1) {\n        qk.fused_smem_iter_A().store(qk_tb_A);\n        qk.fused_smem_iter_B().store(qk_tb_B);\n        if (gemm_k_iterations > 1) {\n          gk.fused_smem_iter_B().store(gk_tb_B);\n        }\n        __syncthreads();   // single shared barrier\n        // QK stage advance (A + B stream).\n        ++qk.fused_smem_iter_A();\n        ++qk.fused_smem_iter_B();\n        if (qk_stage == 1) {\n          qk.fused_smem_iter_A().add_tile_offset({0, -kStages});\n          qk.fused_smem_iter_B().add_tile_offset({-kStages, 0});\n        } else {\n          qk.fused_warp_iter_A().add_tile_offset(\n              {0, -kStages * kPartitionsK * kWarpGemmIterations});\n          qk.fused_warp_iter_B().add_tile_offset(\n              {-kStages * kPartitionsK * kWarpGemmIterations, 0});\n        }\n        qk_stage ^= 1;\n        // GradK stage advance (B only; A=T fixed).\n        ++gk.fused_smem_iter_B();\n        if (gk_stage == 1) {\n          gk.fused_smem_iter_B().add_tile_offset({-kStages, 0});\n        } else {\n          gk.fused_warp_iter_B().add_tile_offset(\n              {-kStages * kPartitionsK * kWarpGemmIterations, 0});\n        }\n        gk_stage ^= 1;\n        gk_hasNext = gemm_k_iterations > 1;\n      }\n\n      qk.fused_warp_iter_A().set_kgroup_index((warp_mma_k + 1) % kWarpGemmIterations);\n      qk.fused_warp_iter_B().set_kgroup_index((warp_mma_k + 1) % kWarpGemmIterations);\n      qk.fused_warp_iter_A().load(qk_A[(warp_mma_k + 1) % 2]);\n      qk.fused_warp_iter_B().load(qk_B[(warp_mma_k + 1) % 2]);\n      ++qk.fused_warp_iter_A();\n      ++qk.fused_warp_iter_B();\n\n      if (gk_hasNext) {\n        gk.fused_warp_iter_B().set_kgroup_index((warp_mma_k + 1) % kWarpGemmIterations);\n        gk.fused_warp_iter_A().load(gk_A[(warp_mma_k + 1) % 2]);\n        gk.fused_warp_iter_B().load(gk_B[(warp_mma_k + 1) % 2]);\n        ++gk.fused_warp_iter_A();\n        ++gk.fused_warp_iter_B();\n      }\n\n      if (warp_mma_k == 0) {\n        qk_iter_A.load(qk_tb_A);\n        qk_iter_B.load(qk_tb_B);\n        ++qk_iter_A;\n        ++qk_iter_B;\n        qk_iter_A.clear_mask(gemm_k_iterations <= 2);\n        qk_iter_B.clear_mask(gemm_k_iterations <= 2);\n        if (gk_hasNext) {\n          gk_iter_B.load(gk_tb_B);\n          ++gk_iter_B;\n          gk_iter_B.set_residual_tile(gemm_k_iterations == 3);\n          gk_iter_B.clear_mask(gemm_k_iterations <= 2);\n        }\n      }\n\n      // The two independent warp-mmas: their HMMAs overlap, filling each other's smem-load shadows.\n      qk_warp_mma(s_accum, qk_A[warp_mma_k % 2], qk_B[warp_mma_k % 2], s_accum);\n      gk_warp_mma(gradK_accum, gk_A[warp_mma_k % 2], gk_B[warp_mma_k % 2], gradK_accum);\n    }\n  }\n}\n\n} // namespace fmha_fused",
    "replace": "      bool gk_hasNext = true;\n\n      if (warp_mma_k == kWarpGemmIterations - 1) {\n        FMHA_PHASE(qk, 0/*QK*/) {\n          qk.fused_smem_iter_A().store(qk_tb_A);\n          qk.fused_smem_iter_B().store(qk_tb_B);\n        }\n        FMHA_PHASE(gradk, 8/*GRADK*/) {\n          if (gemm_k_iterations > 1) {\n            gk.fused_smem_iter_B().store(gk_tb_B);\n          }\n        }\n        __syncthreads();   // single shared barrier\n        // QK stage advance (A + B stream).\n        FMHA_PHASE(qk, 0/*QK*/) {\n          ++qk.fused_smem_iter_A();\n          ++qk.fused_smem_iter_B();\n          if (qk_stage == 1) {\n            qk.fused_smem_iter_A().add_tile_offset({0, -kStages});\n            qk.fused_smem_iter_B().add_tile_offset({-kStages, 0});\n          } else {\n            qk.fused_warp_iter_A().add_tile_offset(\n                {0, -kStages * kPartitionsK * kWarpGemmIterations});\n            qk.fused_warp_iter_B().add_tile_offset(\n                {-kStages * kPartitionsK * kWarpGemmIterations, 0});\n          }\n          qk_stage ^= 1;\n        }\n        // GradK stage advance (B only; A=T fixed).\n        FMHA_PHASE(gradk, 8/*GRADK*/) {\n          ++gk.fused_smem_iter_B();\n          if (gk_stage == 1) {\n            gk.fused_smem_iter_B().add_tile_offset({-kStages, 0});\n          } else {\n            gk.fused_warp_iter_B().add_tile_offset(\n                {-kStages * kPartitionsK * kWarpGemmIterations, 0});\n          }\n          gk_stage ^= 1;\n        }\n        gk_hasNext = gemm_k_iterations > 1;\n      }\n\n      FMHA_PHASE(qk, 0/*QK*/) {\n        qk.fused_warp_iter_A().set_kgroup_index((warp_mma_k + 1) % kWarpGemmIterations);\n        qk.fused_warp_iter_B().set_kgroup_index((warp_mma_k + 1) % kWarpGemmIterations);\n        qk.fused_warp_iter_A().load(qk_A[(warp_mma_k + 1) % 2]);\n        qk.fused_warp_iter_B().load(qk_B[(warp_mma_k + 1) % 2]);\n        ++qk.fused_warp_iter_A();\n        ++qk.fused_warp_iter_B();\n      }\n\n      FMHA_PHASE(gradk, 8/*GRADK*/) {\n        if (gk_hasNext) {\n          gk.fused_warp_iter_B().set_kgroup_index((warp_mma_k + 1) % kWarpGemmIterations);\n          gk.fused_warp_iter_A().load(gk_A[(warp_mma_k + 1) % 2]);\n          gk.fused_warp_iter_B().load(gk_B[(warp_mma_k + 1) % 2]);\n          ++gk.fused_warp_iter_A();\n          ++gk.fused_warp_iter_B();\n        }\n      }\n\n      if (warp_mma_k == 0) {\n        FMHA_PHASE(qk, 0/*QK*/) {\n          qk_iter_A.load(qk_tb_A);\n          qk_iter_B.load(qk_tb_B);\n          ++qk_iter_A;\n          ++qk_iter_B;\n          qk_iter_A.clear_mask(gemm_k_iterations <= 2);\n          qk_iter_B.clear_mask(gemm_k_iterations <= 2);\n        }\n        FMHA_PHASE(gradk, 8/*GRADK*/) {\n          if (gk_hasNext) {\n            gk_iter_B.load(gk_tb_B);\n            ++gk_iter_B;\n            gk_iter_B.set_residual_tile(gemm_k_iterations == 3);\n            gk_iter_B.clear_mask(gemm_k_iterations <= 2);\n          }\n        }\n      }\n\n      // The two independent warp-mmas: their HMMAs overlap, filling each other's smem-load shadows.\n      FMHA_PHASE(qk, 0/*QK*/) {\n        qk_warp_mma(s_accum, qk_A[warp_mma_k % 2], qk_B[warp_mma_k % 2], s_accum);\n      }\n      FMHA_PHASE(gradk, 8/*GRADK*/) {\n        gk_warp_mma(gradK_accum, gk_A[warp_mma_k % 2], gk_B[warp_mma_k % 2], gradK_accum);\n      }\n    }\n  }\n  // ПЛОМБЫ обоих накопителей стоят у ВЫЗЫВАЮЩЕГО (kernel_backward.h): здесь оба -- ссылки, и\n  // пломбировать их надо там, где компилятор видит их дальнейшую судьбу.\n}\n\n} // namespace fmha_fused"
   }
  ]
 }
]